import { Components } from "@mui/material";

const MuiCardMedia: Components["MuiCardMedia"] = {
  styleOverrides: {
    root: {
      borderRadius: 20,
      position: "relative",
      overflow: "hidden",
    },
  },
};

export default MuiCardMedia;
